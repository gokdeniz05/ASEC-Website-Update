// ASEC Kulübü - Görsel İşleme Kütüphanesi

// Lightbox için görsel yükleme fonksiyonu
function loadLightboxImage(imageUrl, lightboxImg) {
    // Görseli doğrudan yükle
    lightboxImg.src = imageUrl;
}
